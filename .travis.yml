language: python
python:
  - "3.2"
  - "3.3"
cache:
  - apt
  - directories:
    - $HOME/emacs-24.3
before_install:
  - sudo make travis-dependencies
install:
  - make tangle
  - pip install .
script:
  - make _check
